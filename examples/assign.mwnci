#!/usr/bin/env mwnci
include("math")
//
// This program demonstrates the use of variable-updates without the need
// for `let`.
//

// The let is optional - and goes back to the days of having to use
// it to initialise a variable
let i = 3
i = 2 * PI
print("2xPI ", i, "\n")

x = 3
println("X:", x)


yfds = 33.312;
println("X:", yfds)
