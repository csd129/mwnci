/*
This file is compiled in.
If any definitions are added here then the source will need to be
recompliled.

It will be best to add any defaults to the "main" include file
so no recompilation is needed and it will be loaded at run time
*/

INCLUDE={}
include("main")
